/**
 * Flow row types.
 *
 * Re-exported from the generated database types so there is one definition to
 * keep in step with the migrations, not two.
 */
export type {
  Flow,
  FlowRun,
  FlowRunStatus,
  FlowRunStep,
  FlowStatus,
  FlowVersion,
} from '@/lib/database.types';
